import React, { Component } from "react";
import {Container} from "../../components/Grid";
import "./Play.css";

class Play extends Component {
  state = {
  };

  componentDidMount() {      
  }
  
  render() {
    return (  
        <Container>
        </Container>
    );
  }
}

export default Play;
